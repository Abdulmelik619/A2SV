class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        lst=[]
        for i , j in enumerate(nums):
            if(j==target):
                lst.append(i)
        return lst        
